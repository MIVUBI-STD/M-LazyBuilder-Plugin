# LazyBuilder Plugin

Minecraft Java plugin workspace for LazyBuilder.

Active development happens on the `Local` branch. `main` is the stable/release branch and should only receive promoted, verified work.
