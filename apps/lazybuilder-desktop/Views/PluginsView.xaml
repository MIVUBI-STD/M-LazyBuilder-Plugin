<UserControl x:Class="HaloKaryaMedia.LazyBuilder.Desktop.Views.PluginsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="34">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="Plugins" FontSize="28" FontWeight="SemiBold"/>
                <TextBlock Text="Install, update, categorize, and keep plugin state clean" Margin="0,6,0,0" Opacity="0.7"/>
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <Button Content="Refresh" Padding="16,9" Margin="0,0,8,0" Command="{Binding RefreshCommand}"/>
                <Button Content="+ Add Plugin" Padding="16,9" Command="{Binding AddCommand}"/>
            </StackPanel>
        </Grid>

        <TextBlock Grid.Row="1" Text="{Binding StatusMessage}" Margin="0,18,0,12" Opacity="0.75" TextWrapping="Wrap"/>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="18"/>
                <ColumnDefinition Width="*" MinWidth="280"/>
            </Grid.ColumnDefinitions>

            <DataGrid Grid.Column="0"
                      ItemsSource="{Binding Plugins}"
                      SelectedItem="{Binding SelectedPlugin}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      SelectionMode="Single"
                      HeadersVisibility="Column"
                      CanUserAddRows="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Plugin" Binding="{Binding DisplayName}" Width="*"/>
                    <DataGridTextColumn Header="Version" Binding="{Binding Version}" Width="110"/>
                    <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="150"/>
                    <DataGridTextColumn Header="Status" Binding="{Binding State}" Width="110"/>
                </DataGrid.Columns>
            </DataGrid>

            <Border Grid.Column="2" Padding="18">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="Plugin Details" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,18"/>
                        <TextBlock Text="{Binding SelectedPlugin.DisplayName, TargetNullValue=Select a plugin}" FontSize="20" FontWeight="SemiBold"/>
                        <TextBlock Text="{Binding SelectedPlugin.Version}" Margin="0,4,0,14" Opacity="0.7"/>

                        <TextBlock Text="Status" FontWeight="SemiBold"/>
                        <TextBlock Text="{Binding SelectedPlugin.State}" Margin="0,4,0,14"/>

                        <TextBlock Text="Problem" FontWeight="SemiBold"/>
                        <TextBlock Text="{Binding SelectedPlugin.ProblemDetail, TargetNullValue=None}" Margin="0,4,0,14" TextWrapping="Wrap"/>

                        <TextBlock Text="Category" FontWeight="SemiBold"/>
                        <ComboBox ItemsSource="{Binding Categories}" SelectedItem="{Binding SelectedCategory}" Margin="0,6,0,8"/>
                        <Button Content="Apply Category" Padding="12,8" HorizontalAlignment="Left" Command="{Binding ApplyCategoryCommand}" Margin="0,0,0,18"/>

                        <StackPanel Orientation="Horizontal" Margin="0,0,0,18">
                            <Button Content="{Binding ToggleEnabledLabel}" Padding="12,8" Margin="0,0,8,0" Command="{Binding ToggleEnabledCommand}"/>
                            <Button Content="Update" Padding="12,8" Margin="0,0,8,0" Command="{Binding UpdateCommand}"/>
                            <Button Content="Remove" Padding="12,8" Command="{Binding RemoveCommand}"/>
                        </StackPanel>

                        <TextBlock Text="Duplicate Resolution" FontWeight="SemiBold"/>
                        <ComboBox ItemsSource="{Binding SelectedPlugin.CandidateFiles}"
                                  SelectedItem="{Binding SelectedDuplicateFile}"
                                  Margin="0,6,0,8"/>
                        <Button Content="Keep Selected JAR"
                                Padding="12,8"
                                HorizontalAlignment="Left"
                                Command="{Binding ResolveDuplicatesCommand}"/>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</UserControl>
